# Final review — audit conformance, API sweep, docs execution, spec completeness

Date started: 2026-07-12. Plan: [docs/plans/phase-final-review.md](../plans/phase-final-review.md).

## A. Math-audit conformance (docs/math_audit_final.md §2 + §3)

| Audit item | Status | Evidence |
|---|---|---|
| §2.1 fitted-null MC (not exact), +1-rank p, honest split | HONORED | `src/natex/validate/randomization.py:1-7` docstring states parametric bootstrap "NOT an exact randomization test"; `:75` pins `p = (1 + #{null >= obs}) / (Q + 1)`. `src/natex/validate/panel.py:157-173` states the same, `:224-227` every replica refits its own background, `:230` same +1-rank formula. Honest split: `src/natex/validate/honest.py:9-14` (`honest_split`), wired into `src/natex/iv/pipeline.py:100` and documented as the primary post-selection guarantee (`docs/method_cards/lord3.md:37-40`). Grep of `src/natex` finds no exactness claim in any docstring/output string. Tests: `tests/test_did_validation.py:209` (`test_plus_one_rank_exact_value`, pins (1+0)/(4+1)), `tests/test_randomization.py:13,21`, `tests/test_validation.py:10`. |
| §2.2 direct Bernoulli(p̂) null replicas | HONORED | `src/natex/validate/randomization.py:29-32` `_draw_null_treatment` calls `rng.binomial(1, p_hat)` (fitted mean clipped to [1e-6, 1-1e-6] at `:61`); `src/natex/validate/panel.py:213-214` `rng.binomial(1, background.fitted)`. The legacy `1{p̂+σ̂Z>U}` generator appears nowhere in `src/natex`. Tests: `tests/test_randomization.py:29` (`test_bernoulli_replicas_are_bernoulli` — mean ≈ .1, explicitly "NOT ~0.176 like the legacy generator"), `tests/test_did_validation.py:240` (`test_bernoulli_auto_null`). |
| §2.3 intercept-continuity placebos, side trends, Holm, categorical | HONORED-WITH-CAVEAT (F-A1) | `src/natex/validate/placebo.py:42-68` `placebo_tests`: design `[1, side, s, s*side]` (`:49`), tests the side-intercept jump `beta[1]` (`:55-58`) with HC1 errors (`:20-27` `hc1_ols`), Holm step-down across covariates (`:59-66`), forcing columns excluded (`:46`), categorical covariates expanded per level via `pd.get_dummies` (`:45`) so the local distribution is tested level-by-level inside one Holm family. Tests: `tests/test_validation.py:28` (`test_placebo_passes_on_clean_synthetic`). Caveat: the inline Holm at `:59-66` is not NaN-aware — a NaN p contaminates adjusted values (finding F-A1); no pinning test covers the Holm/categorical path. |
| §2.4 frozen side-indicator 2SLS primary; `W = T − μ` absent | HONORED | `src/natex/estimate/local2sls.py:1-15` module contract; `:116-156` `local_2sls` uses the FROZEN `d.group1` side indicator as the excluded instrument (`:128-134`) with side-specific trend controls `[1, s, s*g]` (`:132`). `src/natex/estimate/iv2sls.py:3-5`: the printed group-instrument form "is inconsistent and is never implemented". Grep confirms no `W = T − μ` estimator anywhere in `src/natex` — mentions are quarantine docstrings only. Tests: `tests/test_estimate.py:15,23`, `tests/test_iv2sls.py:19,33`. |
| §2.5 two-sided studentized τ̂ placebo, +1 rank, matched shapes | HONORED | `src/natex/did/effects.py:419-540` `tau_randomization_test`: two-sided studentized `\|τ_p/se_p\|` vs `\|τ̂/se\|` (`:512`, `:525`), +1-rank p-value (`:525`), matched subset shapes — same number of full profiles, same constrained dimensions, same T0 (`:428-433`, `:496-511`) — with `s_tau` records removed from the placebo panel (`:483-485`); the thesis's "independence of the two tests" claim replaced by the precise conditional statement (`:452-457`). Tests: `tests/test_did_effects.py:282` (negative effect rejects), `:296` (+1-rank exact), `:355` (matched-shapes gess regression), `:531` (matched shapes/metadata). |
| §2.6 density test: frozen normal, honest split, falsification-only | HONORED-WITH-CAVEAT (F-A2) | `src/natex/validate/density.py:1-5` docstring: valid only for the FROZEN discovered geometry, "Use with honest splitting"; `:25-49` `density_test` runs a binned-Poisson McCrary-style jump test on the signed distance along the frozen `d.normal` (`signed_distance`, `src/natex/validate/placebo.py:30-32`); `docs/method_cards/lord3.md:54-56` documents it as a falsification test only. Caveats (F-A2): a binned-Poisson McCrary analogue, not rddensity's local-polynomial estimator; `study`/`discover` pipelines call it on the full discovery sample (`src/natex/cli.py:364`, `src/natex/discover.py:269`) — `honest_split` is wired only into `iv/pipeline.py:100`. Tests: `tests/test_validation.py:37` (`test_density_smoke`). |
| §2.7 DEE Theorem-1 √2 factor / corrected variance | HONORED (doc + conservative implementation) | Recording which variant (plan allowance): natex NEVER computes the paper's printed bound — `src/natex/dee/vknn.py:15-17` "natex never uses the paper's uncorrected printed bound"; `:232-244` `experiment_radius` returns only the diagnostic radius ρ, citing the corrected bound `E[q²] ≤ 2ρ²` (√2 factor, fixed-shape-scaling-only caveat), surfaced as `DEEResult.diagnostics["radii"]` (`src/natex/dee/debias.py:215`); `docs/method_cards/dee.md` §"Item 7" documents the corrected route and both caveats. Tests: `tests/test_vknn.py:199` (`test_experiment_radius_hand_computed`). |
| §2.8 one model label per draw; mixture covariance | HONORED | `src/natex/dee/bma.py:318` pins `cov = w Σ_a + (1−w) Σ_b + w(1−w)(μ_a−μ_b)(μ_a−μ_b)ᵀ` exactly; `:276-292` `MixturePosterior.sample` draws ONE Bernoulli(w) model label per posterior draw (`:288`) and the whole draw from that component. Tests: `tests/test_dee_bma.py:97` (`test_mixture_covariance_audit8_formula`), `:145` (`test_label_per_draw_aggregate_variance_regression_audit8`), `:158` (MC vs analytic). |
| §2.9 cross-fit obs vs IV stage; SE² never known independent noise | HONORED-WITH-CAVEAT (F-A3) | `src/natex/dee/observational.py:136-173` `experiment_crossfit_cate`: fold-f model trains on rows excluding EVERY fold-mate experiment's members, in particular its own (`:163-168`); wired at `src/natex/dee/debias.py:192`. SE² are estimated, never known: `src/natex/dee/noise.py:1-25` chi-square measurement model on log SE² (known bias ψ(df/2)−log(df/2), known variance ψ₁(df/2)); default `smooth_noise_stage1=True` (`src/natex/dee/debias.py:132`, `:205-207`). Caveat (F-A3): the documented opt-out `smooth_noise_stage1=False` feeds raw HC1 SE² directly as known GP noise (`:208-209`). Tests: `tests/test_dee_observational.py:238` (`test_poisoned_member_leak_regression`), `:196` (fold coverage), `tests/test_dee_noise.py:18,54`. |
| §2.10 first-stage relevance after validation/repair; AR/Fieller surfaced | HONORED | `src/natex/estimate/local2sls.py:141` first-stage jump/t computed on every estimate (`EffectEstimate.first_stage_t`/`weak_instrument`, `:36-41`); `:146` AR/Fieller set with the frozen oriented instrument (`ar_ci`/`ar_kind`). `src/natex/estimate/iv2sls.py:289` HC1 Wald F + partial R² (`_first_stage_diagnostics`, `:102-126`), `:292-303` closed-form AR set with four honest outcomes, `:311` weak flag (NaN F → True). Checked AFTER repair/validation: `src/natex/dee/debias.py:172-181` effects follow `voronoi_knn_repair` + `balance_filter` (weak instruments kept, downweighted — `:30-32`); `src/natex/discover.py:266-297` effects follow randomization/placebo/density. Relevance never inferred from a high residual LLR: `iv2sls.py:5-8`, `docs/method_cards/lord3.md:51-53`. Tests: `tests/test_ar_ci.py` (`:99` never-fabricated interval, `:116` boundedness iff F > F_crit, `:174` weak-IV coverage), `tests/test_iv2sls.py:139,151`. |

Status vocabulary: HONORED / HONORED-WITH-CAVEAT / NOT-HONORED / DOC-ONLY (typo items:
documented in a method card, no code impact).

## B. API consistency sweep

## C. Docs accuracy execution log

| Doc | Command executed | Expected (as documented) | Observed | Verdict |
|---|---|---|---|---|

## D. Spec completeness matrix

| Spec section | Promise | Disposition | Evidence / future_work entry |
|---|---|---|---|

## Findings register

| ID | Source | Severity | Where | Defect | Resolution |
|---|---|---|---|---|---|
| F-A1 | task 2, audit §2 item 3 | must-fix | `src/natex/validate/placebo.py:59-66` | The inline Holm step-down is not NaN-aware: a NaN placebo p (reachable — a dummy exactly collinear with the side indicator gives se=0 → t=NaN → p=NaN at `:56-58`) inherits the running max as a fabricated finite adjusted p (silently counted by `passed`), or yields a literal 0.0 when the NaN sorts first, and inflates the Holm multiplier m for real p-values. Verified by direct execution of the `:59-66` logic with `{a: .02, b: NaN, c: .5}` → `p_holm[b] = 0.06`, and `{b: NaN, a: .02}` → `p_holm[b] = 0.0`. Violates NaN-never-0.0 and item-3 multiplicity correctness. NaN-aware implementations already exist at `src/natex/did/effects.py:559-568` and `src/natex/validate/panel.py:311-325`; repair = reuse one + pinning test (also covers the untested categorical/Holm path). | |
| F-A2 | task 2, audit §2 item 6 | noted | `src/natex/validate/density.py:25`; `src/natex/cli.py:364`; `src/natex/discover.py:269` | Audit prescribes "rddensity on signed distance along the frozen normal from an honest split": natex implements a binned-Poisson McCrary-style analogue (not rddensity's local-polynomial estimator — core-deps constraint, deviation documented in `density.py:1-5` and `lord3.md:54-56`), and the `study`/`discover` pipelines run it on the full discovery sample; `honest_split` exists (`validate/honest.py`) but is wired only into `iv/pipeline.py:100`. Mitigation: documented as a falsification test only, never confirmatory. | |
| F-A3 | task 2, audit §2 item 9 | noted | `src/natex/dee/debias.py:148-150,208-209` | The `smooth_noise_stage1=False` opt-out feeds raw HC1 SE² directly to the GPs as known per-point noise — the pattern audit item 9 forbids. It is a documented, non-default comparison mode; the default (`True`) uses the adopted chi-square measurement model (`dee/noise.py`). | |

## Fixes applied

## Run of record
