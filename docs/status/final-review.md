# Final review — audit conformance, API sweep, docs execution, spec completeness

Date started: 2026-07-12. Plan: [docs/plans/phase-final-review.md](../plans/phase-final-review.md).

## A. Math-audit conformance (docs/math_audit_final.md §2 + §3)

| Audit item | Status | Evidence |
|---|---|---|
| §2.1 fitted-null MC (not exact), +1-rank p, honest split | HONORED | `src/natex/validate/randomization.py:1-7` docstring states parametric bootstrap "NOT an exact randomization test"; `:75` pins `p = (1 + #{null >= obs}) / (Q + 1)`. `src/natex/validate/panel.py:157-173` states the same, `:224-227` every replica refits its own background, `:230` same +1-rank formula. Honest split: `src/natex/validate/honest.py:9-14` (`honest_split`), wired into `src/natex/iv/pipeline.py:100` and documented as the primary post-selection guarantee (`docs/method_cards/lord3.md:37-40`). Grep of `src/natex` finds no exactness claim in any docstring/output string. Tests: `tests/test_did_validation.py:209` (`test_plus_one_rank_exact_value`, pins (1+0)/(4+1)), `tests/test_randomization.py:13,21`, `tests/test_validation.py:10`. |
| §2.2 direct Bernoulli(p̂) null replicas | HONORED | `src/natex/validate/randomization.py:29-32` `_draw_null_treatment` calls `rng.binomial(1, p_hat)` (fitted mean clipped to [1e-6, 1-1e-6] at `:61`); `src/natex/validate/panel.py:213-214` `rng.binomial(1, background.fitted)`. The legacy `1{p̂+σ̂Z>U}` generator appears nowhere in `src/natex`. Tests: `tests/test_randomization.py:29` (`test_bernoulli_replicas_are_bernoulli` — mean ≈ .1, explicitly "NOT ~0.176 like the legacy generator"), `tests/test_did_validation.py:240` (`test_bernoulli_auto_null`). |
| §2.3 intercept-continuity placebos, side trends, Holm, categorical | HONORED-WITH-CAVEAT (F-A1) | `src/natex/validate/placebo.py:42-68` `placebo_tests`: design `[1, side, s, s*side]` (`:49`), tests the side-intercept jump `beta[1]` (`:55-58`) with HC1 errors (`:20-27` `hc1_ols`), Holm step-down across covariates (`:59-66`), forcing columns excluded (`:46`), categorical covariates expanded per level via `pd.get_dummies` (`:45`) so the local distribution is tested level-by-level inside one Holm family. Tests: `tests/test_validation.py:28` (`test_placebo_passes_on_clean_synthetic`). Caveat: the inline Holm at `:59-66` is not NaN-aware — a NaN p contaminates adjusted values (finding F-A1); no pinning test covers the Holm/categorical path. |
| §2.4 frozen side-indicator 2SLS primary; `W = T − μ` absent | HONORED | `src/natex/estimate/local2sls.py:1-15` module contract; `:116-156` `local_2sls` uses the FROZEN `d.group1` side indicator as the excluded instrument (`:128-134`) with side-specific trend controls `[1, s, s*g]` (`:132`). `src/natex/estimate/iv2sls.py:3-5`: the printed group-instrument form "is inconsistent and is never implemented". Grep confirms no `W = T − μ` estimator anywhere in `src/natex` — mentions are quarantine docstrings only. Tests: `tests/test_estimate.py:15,23`, `tests/test_iv2sls.py:19,33`. |
| §2.5 two-sided studentized τ̂ placebo, +1 rank, matched shapes | HONORED | `src/natex/did/effects.py:419-540` `tau_randomization_test`: two-sided studentized `\|τ_p/se_p\|` vs `\|τ̂/se\|` (`:512`, `:525`), +1-rank p-value (`:525`), matched subset shapes — same number of full profiles, same constrained dimensions, same T0 (`:428-433`, `:496-511`) — with `s_tau` records removed from the placebo panel (`:483-485`); the thesis's "independence of the two tests" claim replaced by the precise conditional statement (`:452-457`). Tests: `tests/test_did_effects.py:282` (negative effect rejects), `:296` (+1-rank exact), `:355` (matched-shapes gess regression), `:531` (matched shapes/metadata). |
| §2.6 density test: frozen normal, honest split, falsification-only | HONORED-WITH-CAVEAT (F-A2) | `src/natex/validate/density.py:1-5` docstring: valid only for the FROZEN discovered geometry, "Use with honest splitting"; `:25-49` `density_test` runs a binned-Poisson McCrary-style jump test on the signed distance along the frozen `d.normal` (`signed_distance`, `src/natex/validate/placebo.py:30-32`); `docs/method_cards/lord3.md:54-56` documents it as a falsification test only. Caveats (F-A2): a binned-Poisson McCrary analogue, not rddensity's local-polynomial estimator; `study`/`discover` pipelines call it on the full discovery sample (`src/natex/cli.py:364`, `src/natex/discover.py:269`) — `honest_split` is wired only into `iv/pipeline.py:100`. Tests: `tests/test_validation.py:37` (`test_density_smoke`). |
| §2.7 DEE Theorem-1 √2 factor / corrected variance | HONORED (doc + conservative implementation) | Recording which variant (plan allowance): natex NEVER computes the paper's printed bound — `src/natex/dee/vknn.py:15-17` "natex never uses the paper's uncorrected printed bound"; `:232-244` `experiment_radius` returns only the diagnostic radius ρ, citing the corrected bound `E[q²] ≤ 2ρ²` (√2 factor, fixed-shape-scaling-only caveat), surfaced as `DEEResult.diagnostics["radii"]` (`src/natex/dee/debias.py:215`); `docs/method_cards/dee.md` §"Item 7" documents the corrected route and both caveats. Tests: `tests/test_vknn.py:199` (`test_experiment_radius_hand_computed`). |
| §2.8 one model label per draw; mixture covariance | HONORED | `src/natex/dee/bma.py:318` pins `cov = w Σ_a + (1−w) Σ_b + w(1−w)(μ_a−μ_b)(μ_a−μ_b)ᵀ` exactly; `:276-292` `MixturePosterior.sample` draws ONE Bernoulli(w) model label per posterior draw (`:288`) and the whole draw from that component. Tests: `tests/test_dee_bma.py:97` (`test_mixture_covariance_audit8_formula`), `:145` (`test_label_per_draw_aggregate_variance_regression_audit8`), `:158` (MC vs analytic). |
| §2.9 cross-fit obs vs IV stage; SE² never known independent noise | HONORED-WITH-CAVEAT (F-A3) | `src/natex/dee/observational.py:136-173` `experiment_crossfit_cate`: fold-f model trains on rows excluding EVERY fold-mate experiment's members, in particular its own (`:163-168`); wired at `src/natex/dee/debias.py:192`. SE² are estimated, never known: `src/natex/dee/noise.py:1-25` chi-square measurement model on log SE² (known bias ψ(df/2)−log(df/2), known variance ψ₁(df/2)); default `smooth_noise_stage1=True` (`src/natex/dee/debias.py:132`, `:205-207`). Caveat (F-A3): the documented opt-out `smooth_noise_stage1=False` feeds raw HC1 SE² directly as known GP noise (`:208-209`). Tests: `tests/test_dee_observational.py:238` (`test_poisoned_member_leak_regression`), `:196` (fold coverage), `tests/test_dee_noise.py:18,54`. |
| §2.10 first-stage relevance after validation/repair; AR/Fieller surfaced | HONORED | `src/natex/estimate/local2sls.py:141` first-stage jump/t computed on every estimate (`EffectEstimate.first_stage_t`/`weak_instrument`, `:36-41`); `:146` AR/Fieller set with the frozen oriented instrument (`ar_ci`/`ar_kind`). `src/natex/estimate/iv2sls.py:289` HC1 Wald F + partial R² (`_first_stage_diagnostics`, `:102-126`), `:292-303` closed-form AR set with four honest outcomes, `:311` weak flag (NaN F → True). Checked AFTER repair/validation: `src/natex/dee/debias.py:172-181` effects follow `voronoi_knn_repair` + `balance_filter` (weak instruments kept, downweighted — `:30-32`); `src/natex/discover.py:266-297` effects follow randomization/placebo/density. Relevance never inferred from a high residual LLR: `iv2sls.py:5-8`, `docs/method_cards/lord3.md:51-53`. Tests: `tests/test_ar_ci.py` (`:99` never-fabricated interval, `:116` boundedness iff F > F_crit, `:174` weak-IV coverage), `tests/test_iv2sls.py:139,151`. |
| §2.11 Alg 6 global incumbent across windows/restarts | HONORED | `src/natex/did/suddds.py:341-363`: one incumbent LLR plus every restart's converged local optimum recorded across ALL windows and restarts; `:365-375` dedup on `(mask bytes, t0)` keeping the max LLR, ranked LLR-descending; `:389` asserts `discoveries[0].llr == incumbent_llr` (audit-11 invariant); contract in the module docstring `:8-11`. Test: `tests/test_suddds.py:167` (`test_global_incumbent_across_windows` — W=3 optimum survives W=2 being processed last; `[0]` == max over all recorded optima; ranked + deduped pinned). |
| §2.12 Alg 7 minimum precision mass both sides of every cutoff | HONORED | `src/natex/did/suddds.py:118-152` `optimize_t0`: exhaustive T0 over in-subset times, a candidate qualifies only with `>= min_side` in-subset records on EACH side inside the window (`:143-146`); no qualifying candidate → `None` (`:152`), the caller keeps its incumbent (`_alternate` `:224-226`) and a cutoff-free restart contributes nothing (`:355-356`); NaN evaluations never selected (`:148-149`). `min_side` default 3, validated `:135-136`. Tests: `tests/test_suddds.py:206` (`test_optimize_t0_min_side`), `:221` (candidates restricted to subset), `:231` (`test_scan_terminates_when_no_cutoff_qualifies`). |
| §2.13 Alg 8 relax-dimension updates retaining the incumbent | HONORED | `src/natex/did/mdss.py:1-10` documents the deletion-only defect and the repair; `:282-286` builds the relaxed state (dimension j unconstrained, others as-is), `:287-301` generates candidate value subsets on the relaxed records, `:307-310` accepts only a STRICT LLR improvement — the incumbent constraint is retained otherwise, so the trace is weakly increasing by construction. Empty-slice priorities sort last but stay reachable (`:28-29`, `:154`, `:181`, `:196`). Tests: `tests/test_mdss.py:277` (`test_incumbent_retention_one_sweep`), `:302` (`test_printed_deletion_only_algorithm_would_have_moved` — pins the repair against the printed algorithm), `:140` (monotone LLR trace, all priorities). |
| §2.14 Alg 9 argmin + control-set MSE initialized +∞ | HONORED | `src/natex/did/controls.py:320` initializes from `s_sup = s_tau` — empty control → `pre_mse = +inf` (`_counterfactual` `:130-135`: no defined pre record → `float("inf")`, never 0); `:341-347` `np.argmin` over candidate MSEs with the audit-14 comment ("the printed argmax is a typo"; +inf failures never beat a finite MSE, NaN never appears — failures are +inf by policy); docstring `:291-301`. Tests: `tests/test_did_controls.py:326` (`test_gess_argmin_regression` — printed argmax would pick the MSE-1 candidate; also pins `trace[0] == +inf`), `:344` (monotone trace + termination), `:431` (empty-control edge). |
| §2.15 single-Δ profile GLR: C̃/B̃, δ̄ correction, window restriction, both signs | HONORED | `src/natex/did/statistics.py:140-180` `single_delta_stats`: per-profile `delta_bar = Σwd/Σw`, `B̃ = Σw − (Σwd)²/Σw`, `C̃ = Σw(d−δ̄)r` — the exact profile-out of μᵢ under BOTH hypotheses (`:149-156`, computed `:166-176`); window restriction is structural (out-of-window records carry `c = b = 0`, `WindowStats` `:44-59`, `:84-89`); `:183-199` `single_delta_llr = C²/(2B)`, MLE `Δ̂ = C/B`, even in Δ; both signs enter via descending AND ascending priority orderings (`src/natex/did/mdss.py:186-199`). The audit's 4.74-vs-5.33 numeric check is pinned as a class, not a constant: `tests/test_did_statistics.py:155` (`test_single_delta_matches_bruteforce_and_beats_unprofiled` — profiled GLR equals the brute-force numeric optimum to 1e-6 and the thesis-printed statistic (μ frozen at H0 MLE, `B = Σw`) is strictly smaller under unbalanced pre/post precision, "the 4.74-vs-5.33 class of counterexample"); `:209` both signs; `:304` window restriction exact equality. |
| §2.16 WCC labeled heuristic; exhaustive 2^V−1 the exact small-cardinality default | HONORED | WCC (and greedy) labeled heuristic: `src/natex/did/mdss.py:12-24` ("At larger cardinality one of three heuristic priority orderings is used"), `src/natex/did/suddds.py:20-21`, `docs/method_cards/suddds.md:43-45` ("both heuristic, **not exact** (audit item 16)"). Exhaustive default: `mdss.py:109-112` (`_nonempty_value_subsets`, all `2^k − 1`), `:287-293` taken whenever `k_j <= exhaustive_max_values` (default 12: `mdss.py:210`, `suddds.py:277`), with a loud refusal above `_MAX_EXACT_VALUES = 20` (`:288-292`) — never a silent cap. Test: `tests/test_mdss.py:171` (`test_exact_branch_equals_brute_force` — exact for the printed statistic). |
| §2.17 Eq 6.10 prose swap (DOC-ONLY) | DOC-ONLY, HONORED | `docs/method_cards/suddds.md:41-43`: "Eq 6.10's **prose** swaps `q1/q2 ↔ beta_g0/beta_g1`; the equations are right (audit item 17) — in this implementation `q1 = C1/B1` is the **post**-side mean." Same statement at the implementation: `src/natex/did/statistics.py:12-14` (module docstring) and `:118-123` (`double_beta_q` docstring). Test: `tests/test_did_statistics.py:119` (empty sides NaN, never 0). |
| §2.18 panel nulls preserve unit/time dependence; NO DiD-in-time McCrary | HONORED | `src/natex/validate/panel.py:19-32`: null kinds — `ar1_unit` (normal-model default: unit random effects + per-unit stationary AR(1), dependence-preserving), `iid` documented DEPENDENCE-BREAKING comparison only, `bernoulli` for the item-2 path; fit/draw `:91-146`; auto selection `:185`. McCrary-on-calendar-time exists nowhere; its replacements are `composition_test` `:253-294` (docstring `:261-263`: "McCrary on calendar time is information-free when record times are design-determined") and `anticipation_test` `:328-406` (pre-period placebo jumps, Holm across shifts, all-degenerate = fail). Every replica refits its own background (`:224-227`). Tests: `tests/test_did_validation.py:179` (`test_ar1_null_vs_iid_dependence_regression`), `:315-344` (composition incl. degenerate-is-NaN-never-ok), `:367-405` (anticipation). |
| §2.19 continuous-treatment estimand + model class matches T's type | HONORED-WITH-CAVEAT (F-B4) | Dose normalization: `src/natex/did/effects.py:180-243` `did_effect` — the raw DD contrast estimates `zeta*tau`, so `tau` is divided by the theta DD contrast `delta_hat` (`_dose_contrast` `:153`, applied `:231-242`); `dose_normalize="auto"` applies iff theta is non-binary (`:211-213`); near-zero dose → `tau = se = NaN`, never 0 (`:195`, `:242`). Model-class matching: `src/natex/did/suddds.py:17-21`, `:313-316` (`model="auto"` → Bernoulli iff theta binary in {0,1}; `bernoulli` on non-binary theta raises), `src/natex/did/background.py:134-139`; the Bernoulli LLR is exact (`src/natex/did/statistics.py:202-227`), working residuals order priorities only (`:230-240`). CLI plumbing: `src/natex/cli.py:305-307` (`--model auto|normal|bernoulli`, help cites "audit-19 model matching"). Tests: `tests/test_did_effects.py:218,236,248,256`; `tests/test_suddds.py:281,293,305,338`. Caveat (F-B4): no Poisson variant for count treatments — documented future work (`docs/method_cards/suddds.md:74-75,179-180`). |
| §2.20 reverse-neighbor variance bug; legacy outputs not σ̂² ground truth | HONORED | `src/natex/scan/neighborhoods.py:53-62` `local_residual_variance` is row-wise over each point's OWN kNN index rows (`np.var(r[idx], axis=1)`), docstring cites item 20; full-geometry variances even when scanning a center subset (`src/natex/rdd/lord3.py:85-87` — every member's OWN-kNN variance). No parity test treats legacy outputs as σ̂² ground truth: `tests/test_variance.py:6` (`test_own_neighborhood_not_reverse_neighbors` pins own ≠ reverse), backtests assert documented dataset truths and say so explicitly (`tests/backtests/test_academic_probation.py:10-12`: "audit item 20: legacy outputs are NOT parity targets"); grep of `tests/` + `tests/backtests/` finds no assertion against legacy scan/σ̂² output (the only "parity" targets are natex-internal reference implementations and published paper truths). Landmine documented: `docs/method_cards/lord3.md:57-63`. |
| §2.21 pure-group bisections scored via boundary likelihood supremum | HONORED | `src/natex/scan/statistics.py:63-75` `offset_log_lik` returns the boundary supremum 0.0 at θ = ±inf for the matching pure group ("audit correction: pure-group splits are scored, never NA"); `:83-89` pure groups → ±inf MLE; vectorized `:137-141` and `:188-190` (audit item 21 cited); `bernoulli_llr_all_splits` `:205-222` scores pure-group columns finitely. Normal analog: `normal_glr_common_variance` `:43-46,57` floors RSS1 at the smallest positive normal float so σ→0 boundary suprema stay finite and rank correctly. DiD kernel: `src/natex/did/statistics.py:211-213`. Tests: `tests/test_statistics_bernoulli.py:22` (`test_pure_groups_get_boundary_supremum`), `tests/test_statistics_bernoulli_vectorized.py:71` (boundary conventions), `:103` (`test_pure_group_boundary_supremum_finite`). |
| §2.22 bracketed Newton in θ=log β; no bracket-doubling; complement-dedup fixed | HONORED | Scalar: `src/natex/scan/statistics.py:78-105` — Newton on the score inside a FIXED shrinking bracket `(-30, 30)` with bisection fallback (no bracket-doubling construct exists anywhere in `src/natex`); vectorized: `:132-178` (docstring cites audit item 22; converged columns retired). θ is the log-odds offset, i.e. log β. Complement dedup: `src/natex/scan/neighborhoods.py:41-49` — the legacy `~col` check was dead code (tied points, at least the center, are group 1 under BOTH orientations); the antipodal key is `dots <= 0.0`. Tests: `tests/test_statistics_bernoulli.py:11` (MLE == grid search), `tests/test_statistics_bernoulli_vectorized.py:56` (vectorized == scalar per column), `tests/test_neighborhoods.py:27` (`test_dedup_complements_and_duplicates`). |
| §2.23 hyperplane tie convention explicit and documented | HONORED | `src/natex/scan/neighborhoods.py:2-4` (module contract: "signed distance >= 0 => group 1; the center point lies on every candidate plane and always belongs to group 1"), applied `:31` (`dots >= 0.0`); reused identically in DEE (`src/natex/dee/vknn.py:156` `group1 = s >= 0.0`; Voronoi ownership ties → earlier-accepted center, `dee.md:71-72`). Documented in BOTH method cards: `docs/method_cards/lord3.md:79-80`, `docs/method_cards/dee.md:71`. Tests: `tests/test_neighborhoods.py:14` (`test_partitions_match_naive_and_center_in_group1`), `tests/test_vknn.py:54`. |
| §2.24 data-scaled variance shrinkage, no absolute floor | HONORED | Scan: `src/natex/scan/neighborhoods.py:58-62` — floor `1e-3 * var(r)` (data-scaled, docstring cites item 24). DiD background: `src/natex/did/background.py:16-20,42,83-103` — per-profile variances shrunk toward the global residual variance with count weight `lam = 10/(10+n_prof)`, final floor `1e-12 * s2_global` ("never an absolute constant"). The only `1e-6` constants in `src/natex` are Bernoulli PROBABILITY clips (`validate/randomization.py:61`, `did/background.py:40`, `rdd/lord3.py:18`), not variance floors. Tests: `tests/test_variance.py:17` (`test_floor_is_data_scaled`), `tests/test_did_background.py:112` (`test_zero_variance_profile_gets_scaled_floor`). |
| §2 pure-typos block (must appear in method cards) | HONORED-WITH-CAVEAT (F-B1) | Present: `x^R→x^r` all three occurrences + Eq 5.18 union→intersection (`docs/method_cards/lord3.md:64-66`); Eq 6.4 rank deficiency (`suddds.md:86-88` + `src/natex/did/background.py:11-15`); Eq 6.18/6.20 summed-record denominators + `s→s_tau` subscript (`suddds.md:94-97`); Eq 6.19 unit-level weights (`suddds.md:98`); Eq 6.21 ill-typed (`suddds.md:99-100`); Eqs 6.24–6.25 dimensionally invalid (`suddds.md:101-102`, corrected DGP in `natex.data.synthetic_did`); Eq 6.27 heteroskedasticity-not-correlation (`suddds.md:103-104`, `src/natex/data/synthetic_did.py:201`); Eq 6.28 set notation (`suddds.md:105`); DEE bias-sign drift Codex #30 (`dee.md:78-89`, pinned by `test_sign_convention_regression`); M vs M′ (`dee.md:212-214`); weighted-BLOOCV mixture weights (`dee.md:215-217`). MISSING from the cards (aggregated finding F-B1): Eq 5.21 inverse-logit + log μᵢ (documented only at `src/natex/data/synthetic.py:1-2`, absent from `lord3.md`); thesis xref 6.26→5.22 (absent from `suddds.md`); DEE Codex #31 (σᵢ SD-vs-variance in LOOCV prose), #34 (Algorithm 2 return tuple type), #35 (Assumption 4 → conditional mean independence), #36 (Appendix F.2 α_Y→α_B) — all absent from `dee.md`. |

Status vocabulary: HONORED / HONORED-WITH-CAVEAT / NOT-HONORED / DOC-ONLY (typo items:
documented in a method card, no code impact).

### §3 improvement decisions (one row per audit bullet)

Note: the audit's adopted list has **10** bullets (the plan header said 9 — a miscount);
all 10 + the 4 rejected/downgraded bullets are recorded, 14 rows.

| §3 decision | Status | Evidence |
|---|---|---|
| Adopted 1: unknown-variance GLR `k/2·log(RSS0/RSS1)`; heteroskedastic plug-in kept as option with caveats | HONORED-WITH-CAVEAT (F-B2) | Kernel implemented exactly: `src/natex/scan/statistics.py:39-60` (`normal_glr_common_variance`, exact-monotone-in-t² note + boundary-supremum floor); tested `tests/test_statistics_normal.py:40`. Caveat: the kernel is NOT selectable from `lord3_scan`/CLI — the only wired Normal scan kernel is the heteroskedastic plug-in `normal_llr_all_splits` (`src/natex/rdd/lord3.py:113`), inverting the adopted default/option polarity (F-B2, noted). |
| Adopted 2: side-specific local-linear alternative, slopes under BOTH hypotheses | HONORED | `src/natex/validate/placebo.py:42-58`: design `[1, side, s, s*side]` — the null is the shared-intercept two-slope model (slopes present under both hypotheses), the alternative adds the side-intercept jump `beta[1]` tested with HC1 — a pure kink does not reject (the Codex repair). Estimation analog: `src/natex/estimate/local2sls.py:132` side-specific trend controls `[1, s, s*g]`. Test: `tests/test_validation.py:28`. (Holm NaN defect in this module already filed as F-A1.) |
| Adopted 3: pure-group supremum; bracketed Newton/IRLS in θ=log β; Firth fallback under separation | HONORED | Boundary suprema + bracketed Newton: see §2.21/§2.22 rows (`src/natex/scan/statistics.py:63-75,78-105,132-178`). Firth fallback: separation is handled by penalized likelihood documented as the audit's Firth-style remedy — ridge-penalized logistic on standardized features in BOTH treatment backgrounds (`src/natex/rdd/lord3.py:50-57`, comment "Penalized likelihood under separation follows the audit's remedy (Firth-style)"; `src/natex/did/background.py:21-26,150-156`). Tests: `tests/test_statistics_bernoulli.py:22`, `tests/test_statistics_bernoulli_vectorized.py:103`. |
| Adopted 4: geometry caching across replicas + complement-dedup fix + exact Kmax-NN prefix reuse | HONORED | `src/natex/scan/geometry.py:1-13,24-56` — `ScanGeometry` built once and shared across all Q replicas (bitwise-identical output), lazy partition cache `:38-45`, `shrink(k2)` exact Kmax-NN prefix `:47-51`; complement dedup `src/natex/scan/neighborhoods.py:41-49`. Tests: `tests/test_geometry.py:28` (prefix exact), `:56` (scan-with-geometry identical), `:112` (`test_randomization_bitwise_parity_and_single_knn_build`), `tests/test_neighborhoods.py:27`. |
| Adopted 5: rddensity on frozen signed distance; honest split primary; step-down offered but labeled approximate | HONORED-WITH-CAVEAT (F-A2, F-B5) | Density on the FROZEN normal: `src/natex/validate/density.py:25-49` (binned-Poisson McCrary analogue, not rddensity's local-polynomial — F-A2, core-deps constraint, documented falsification-only `lord3.md:53-56`). Honest split primary: `src/natex/validate/honest.py:1-14` (docstring: "the primary post-selection guarantee ... cleaner than any claim of exactness"), wired `src/natex/iv/pipeline.py:100`, documented `lord3.md:37-40`. Step-down: Holm step-down exists only INSIDE the validation batteries (`validate/panel.py:311-325`, `did/effects.py:559-568`, `validate/placebo.py:59-66`); a per-discovery step-down selective-inference procedure is not offered (F-B5, noted) — the max-LLR fitted-null calibration + honest split stand in, and no doc claims a step-down guarantee. |
| Adopted 6: overlap-aware aggregation via IF/bootstrap covariance; disjointness (VKNN) the simple default | HONORED-WITH-CAVEAT (F-B6) | The adopted simple default is implemented: disjointness by construction — `src/natex/dee/vknn.py:75-116,128-141` (k′-NN ball ∩ Voronoi cell over accepted centers, forward-stepwise with restore-on-reject). Documented: `docs/method_cards/dee.md:159-161` — the influence-function alternative "was considered and **deferred**: disjointness is the simple default and the repair already enforces it" (no overlap-fraction weighting exists anywhere). Test: `tests/test_vknn.py:54` (`test_disjointness_and_ownership`). IF/bootstrap-covariance aggregation deferred → F-B6 (noted, future-work candidate). |
| Adopted 7: buffered predictive stacking (hyperparameters refit within folds) instead of softmax-PLP; one label per draw | HONORED | `src/natex/dee/bma.py:94-123` (`buffered_folds`: spatial train/test buffer, default = median NN distance `:138`), `:144-163` (`buffered_stacking_weights`: BOTH GPs' hyperparameters refit per buffered fold, weight maximizes held-out log mixture density on a deterministic grid — "a proper 1-D optimization, not softmax-PLP"); default `weighting="stacking"` (`src/natex/dee/debias.py:131,147`); NaN weight falls back to `loo_weights` with `detail["stacking_fallback"]` (`debias.py:33-35`). One model label per draw: `bma.py:276-292` (§2.8 row). Tests: `tests/test_dee_bma.py:185,202,251,259,265,272` and `:145`. |
| Adopted 8: hierarchical stage-1 noise — Normal likelihood + chi-square measurement model on SE² | HONORED | `src/natex/dee/noise.py:1-25` (`log SE²` debiased by `ψ(df/2) − log(df/2)`, known variance `ψ₁(df/2)`, smoothed by the exact GP, data-scaled floor on back-transform); default `smooth_noise_stage1=True` (`src/natex/dee/debias.py:132,205-207`); documented as the adopted §3 form — "a Normal likelihood plus a measurement model on SE², not both a t likelihood and a latent variance" (`docs/method_cards/dee.md:122-127`). Tests: `tests/test_dee_noise.py:18,54`. (The documented `False` opt-out is F-A3, noted.) |
| Adopted 9: exhaustive per-dimension enumeration exact MDSS baseline; corrected single-Δ profile GLR fast path; staggered adoption via group-time ATT backend | HONORED-WITH-CAVEAT (F-B3) | Exhaustive exact baseline: §2.16 row (`mdss.py:109-112,287-293`, default threshold 12; `tests/test_mdss.py:171`). Corrected single-Δ fast path is the DEFAULT method: `src/natex/did/suddds.py:270` (`method="single_delta"`), `src/natex/cli.py:302-303`; corrected statistic §2.15 row. Staggered adoption: NOT shipped — `src/natex/did/effects.py:316-319` defines the `DiDEstimatorBackend` protocol with the docstring "Staggered-adoption group-time ATT (Callaway-Sant'Anna) is a FUTURE backend"; only `mean_gap` is registered (`:345`); documented deviation `suddds.md:185-186` → F-B3 (noted). |
| Adopted 10: weak-IV-robust (AR/Fieller) local inference at discovered cutoffs | HONORED | Same evidence as §2.10: `src/natex/estimate/iv2sls.py:292-303` (closed-form AR set, four honest outcomes), `:311` (weak flag); `src/natex/estimate/local2sls.py:146` (`ar_ci`/`ar_kind` on every local estimate with the frozen oriented instrument). Tests: `tests/test_ar_ci.py:99,116,174`. |
| Rejected 1: exact conditional hypergeometric test as scan-wide replacement | HONORED (absent) | `grep -rni "hypergeom" src tests docs` matches ONLY `docs/math_audit_final.md:148` (the decision itself). Not implemented anywhere, not even as the permitted fixed-split diagnostic; no doc claims it. The scan-wide calibration is the fitted-null Monte Carlo (`validate/randomization.py`, `validate/panel.py`). |
| Rejected 2: within-neighborhood label permutation | HONORED (absent) | No permutation-based validation path exists: every `rng.permutation` in `src/natex` is honest-split shuffling (`validate/honest.py:12`), MDSS dimension-order shuffling (`did/mdss.py:278`), or fold assignment (`dee/bma.py:117`, `dee/observational.py:132`). Validation uses the null-model bootstrap as the audit directs. The only doc "permut" hit is the y-blindness statement (`dee.md:162` — permuted OUTCOME leaves discovery bitwise identical), not a label-permutation test; no doc claims permutation validity. |
| Rejected 3: "recompute index sets after recentering" (unnecessary); keep the two real Alg-1 fixes | HONORED | Rejection documented: `docs/method_cards/dee.md:72-76` ("the projection leaves the separating hyperplane unchanged, so index sets are invariant. Index sets are computed **once** and passed by object"); implementation computes members once per acceptance state and reuses the objects (`src/natex/dee/vknn.py:128-160`). Both kept fixes present: the FIRST candidate is thresholded like every other (`vknn.py:108-112,132-141`; test `tests/test_vknn.py:81`), and every input is explicit — no global `df` (`vknn.py:5-6,99-104`). Deterministic direct orthogonal projection replaces the repo's unseeded Gram–Schmidt (`vknn.py:150`, `dee.md:68-71`; test `tests/test_vknn.py:106,127`). |
| Rejected 4 (downgraded): blanket ANN refutation too broad — ANN acceptable if replicas share the approximation; keep as opt-in for scale | HONORED (absent, no default ANN) | No ANN path or dependency exists: `grep -rni "faiss|annoy|approximate nearest|\bann\b" src docs skills README.md` returns nothing; all neighbor queries are exact `scipy.spatial.cKDTree` (`src/natex/scan/neighborhoods.py:16`, `src/natex/dee/vknn.py:122`). No doc claims an ANN mode, so nothing violates the opt-in-only condition; an opt-in ANN backend remains an (unpromised) future-work candidate. |

## B. API consistency sweep

Date: 2026-07-12. Read-only on `src/`; each check records the exact command and its result.

### B1. Generator discipline

- **Legacy-call grep** — `grep -rn "np\.random\.\(seed\|rand\|randn\|choice\|normal\|uniform\|default_rng()\)" src/natex` → **empty (exit 1)**. No global seeding, no legacy `np.random.*` sampler, no argument-less `default_rng()` anywhere in `src/natex`.
- The only other `np.random.` attribute use is `np.random.SeedSequence(self.seed)` (`src/natex/dee/observational.py:90`) — deterministic child-seed derivation from an explicit int field, no global state.
- **`default_rng(` call sites** (`grep -rn "default_rng" src/natex` → 15 hits, all with an explicit seed): CLI boundary conversions (`src/natex/cli.py:176,261,351,449,583,814` — the plan-permitted convert-ONCE points), spawn-key benchmark design (`src/natex/benchmarks.py:133,193`, documented `:12`), guidance-eval paired arms (`src/natex/guidance_eval.py:153-159`, documented `:147`), and the plan-carried subsample seed (`src/natex/intake/prep.py:184`, deliberately NOT the pipeline Generator so serialized plans replay bitwise — comment `:182-183`).
- **Enumeration** (AST walk over `src/natex`): **48 `rng` parameters** (all annotated `np.random.Generator` or `... | None`) across `data/synthetic*`, `scan/`, `rdd/`, `did/`, `dee/`, `iv/`, `validate/`, `intake/analyst`, `discover`, `guidance_eval`, `benchmarks`; **14 `seed` parameters**: 6 CLI options (boundary), 2 benchmark entry points (spawn keys), `dee/forest.py:49` + `dee/observational.py:57` (sklearn/econml `random_state`, "callers derive it from the pipeline Generator" — `forest.py:40`, `observational.py:90`), `intake/prep.py:43` (plan-carried, documented), `report/bundle.py:128` (metadata only), `guidance_eval.py:139` (top-level, converted once).
- **`rng=None` handling**: 17 explicit `raise ValueError` sites (`grep -rn "rng is None" src/natex` — every hit raises) plus `_require_rng` (`dee/gp.py:32`, ValueError + TypeError) used at 16 call sites across `dee/`. Deferred-requirement sites raise exactly when randomness is consumed: `did/effects.py:480` (`mode=="sample"`), `iv/pipeline.py:90` (`honest=True`), `iv/search.py:145` (`lam=="cv"`); `did/effects.py:571 placebo_dimension_tests` passes through to `tau_randomization_test`.
- `grep -rn "random_state" src/natex` → 5 hits, all pinned from `seed`/`rng` (`observational.py:101`, `forest.py:92`, `iv/search.py:214` `KFold(random_state=int(rng.integers(...)))`).
- **Finding F-C3**: `lord3_scan` (`src/natex/rdd/lord3.py:64-72`) accepts `rng` but its body never uses it — the scan is deterministic; callers (`discover.py:263`, `cli.py:360,450`, `validate/randomization.py:71`) pass `rng=rng` into a dead parameter.

### B2. NaN-never-0.0

- `grep -rn "except:" src/natex` → empty; every `except <Type>` block either re-raises, exits (`typer.Exit`), records an explicit error state, or falls back with the failure logged — spot-checked all 38 `except ` hits. Key sites: `discover.py:458-461` (`rec.status="failed"`, "llr/p stay None — never fabricated"), `intake/analyst.py:194-198` (fallback logged into `errors`), `dee/gp.py:82` (re-raises past max jitter), `report/paper.py:220-227` (compile failure → `(None, False, message)`).
- `grep -rn "return 0\.0" src/natex` → 5 hits, each a **genuine mathematical zero**: `did/effects.py:413` (`tau==0 and se==0` = provably zero movement, docstring `:404-411`; any other degenerate case → NaN `:415`), `scan/statistics.py:71,73` (pure-group boundary supremum, audit item 21), `rdd/metrics.py:14` (entropy of a degenerate distribution), `:22` (NIG with zero base entropy — nothing to explain; benchmark metric, not an estimator). `dee/vknn.py:182` `return 0` is a documented legal count ("Zero is a legal answer" `:178`).
- Zero-fallback suspects adjudicated: `validate/randomization.py:73` + `validate/panel.py:228` (`else 0.0` on an empty replica scan) are genuine — an empty discovery list means every center hit the homogeneous fast path whose scores are provably all 0.0 (`rdd/lord3.py:98-105`); `did/statistics.py:114,196,227` + `scan/statistics.py:222` map *degenerate splits* to the null score 0.0 in LLR kernels (correct ranking convention, audit item 21); `sqrt(max(·, 0.0))` clamps (`placebo.py:27`, `density.py:46`, `local2sls.py:149,181`, `iv2sls.py:287`, `cli.py:469`) guard round-off-negative variances — a resulting `se=0` drives p→0, i.e. fail-loud, never a fabricated success; `panel.py:115-116` φ=0 for a zero-variance unit is the neutral AR(1) fit, not a failure path. `dee/gp.py:138-139` returns `(1e25, zeros(3))` on Cholesky failure — an optimizer barrier value inside hyperparameter search (restarts continue), never a reported result.
- `grep -rn "nan_to_num\|fillna(0" src/natex` → empty; 95 `float("nan")`/`np.nan` failure-path hits; `natex.jsonutil.jsonable` maps non-finite → JSON `null`, never 0 (`src/natex/jsonutil.py:23-25`).

### B3. Result dataclass conventions

| Dataclass | Effect fields | Open-ended dict | Serialization | Verdict |
|---|---|---|---|---|
| `LoRD3Result` (`rdd/lord3.py:34`) | none (discovery-only — never reads the outcome) | — | summarized into results payloads via `jsonable` | OK |
| `SuDDDSResult` (`did/suddds.py:83`) | none (discovery-only) | — | same | OK |
| `CoarseToFineResult` (`scan/coarse.py:28`) | none | `params: dict` (records requested budgets) | same | OK |
| `ControlResult` (`did/controls.py:55`) | counterfactual `y0_hat`, `pre_mse` (no τ by design) | `extras: dict` | same | OK |
| `VKNNResult` (`dee/vknn.py:55`) | none (repair stage) | — (effective `k_prime` a named field) | same | OK |
| `DEEResult` (`dee/debias.py:76`) | `effects: list[EffectEstimate]` (τ/se/ci carriers) | `diagnostics: dict[str, Any]` (extras-style) | GP handles excluded from payloads; CLI writes summary dicts | OK |
| `InstrumentSearchResult` (`iv/search.py:60`) | none (selection never reads the outcome); `first_stage_F`/`partial_r2`/`weak` diagnostics | `extras: dict` | same | OK |
| `DonorSelectionResult` (`iv/donors.py:57`) | `att_post`, `effect_by_time`, `pre_rmspe`/`post_rmspe` (SC-literature ATT naming; no `se`/`ci` — inference via `sc_placebo_test` p) | `extras: dict` | same | F-C5 (noted) |
| `PaperResult` (`report/paper.py:63`) | none (paths + `compiled` + `message`) | — | paths written as strings by the CLI | OK |
| `ResultsBundle` (`report/bundle.py:108`) | plain class (not a dataclass) wrapping the results dict | the dict itself | `jsonable` applied exactly once in `save()` (`bundle.py:254`, contract `:6-8`) | OK |

Effect carriers: `EffectEstimate` (`estimate/local2sls.py`) `tau`/`se`/`ci` + `ar_ci` ✓; `IVEstimate` (`estimate/iv2sls.py:39-54`) `tau`/`se`/`ci` + `extras` ✓; `DiDEffect` (`did/effects.py`) `tau`/`se` + `extras` (no `ci` field — inference is the randomization p, documented) ✓. No ad-hoc synonyms (`effect`, `estimate`, `beta`) exist as effect field names; `grep -rn "pickle" src/natex` → **empty** (no pickled namespaces).

### B4. CLI flag consistency (9 commands, `src/natex/cli.py`)

Extracted by AST walk over every `@app.command` (options, defaults, `help=` presence, docstrings):

| Command | Docstring | `--seed` | `--out` | Options missing `help=` |
|---|---|---|---|---|
| `datasets` (:58) | yes | n/a (deterministic) | n/a | none |
| `fetch-data` (:75) | yes | n/a | n/a (`--root`) | none |
| `study` (:153) | yes | 0 ✓ | `Path("out")` ✓ | `seed`, `out` |
| `discover` (:281) | **NO** (F-C1) | 0 ✓ | `Path("out")` ✓ | `outcome`, `k`, `seed`, `out` |
| `debias` (:414) | yes | 0 ✓ | `Path("out")` ✓ | `treatment`, `seed`, `out` |
| `instruments` (:537) | yes | 0 ✓ | `Path("out")` ✓ | `treatment`, `seed`, `out` |
| `donors` (:654) | yes | n/a (`select_donors`/`sc_placebo_test` are rng-free by construction) | `Path("out")` ✓ | `outcome`, `unit`, `time`, `treated_unit`, `out` |
| `paper` (:889) | yes | n/a (deterministic) | `None` → `BUNDLE/paper`, documented in help ✓ | none |
| `brief` (:920) | yes | n/a | `None` → `BUNDLE/research-brief.md`, documented ✓ | none |

- Shared spellings consistent everywhere they recur: `--treatment` (discover/debias/instruments), `--outcome` (discover/debias/instruments/donors), `--forcing` (discover/debias), `--workdir` + `--backend` + `--model` (study/discover), `--unit`/`--time` (discover-did/donors), `--k`/`--q`/`--degree` (discover/debias). `--seed` defaults to 0 on all four stochastic commands; no seedless stochastic command exists.
- **Finding F-C1** (= plan's known nit 2, fix cross-referenced to task 7): `def discover` has no docstring → empty description in `natex --help`.
- **Finding F-C2**: 17 options across 5 commands lack `help=` (table above; `--seed`/`--out` uniformly undocumented on the data commands, plus 7 required/core options on discover/debias/instruments/donors).

### B5. No bare except

- `grep -rn "except:" src tests` → **empty (exit 1)**.
- `uv run ruff check --select E722 src tests` → **"All checks passed!"**.
- Advisory: `uv run ruff check --select BLE001 src` → **"All checks passed!"** (zero blind-except hits; nothing to report).

### B6. Silent-cap audit (`min(`/`max(`/`np.clip` on user-supplied sizes/budgets)

`grep -rn "min(\|max(\|np\.clip\|\.clip(" src/natex` — every hit touching a user parameter adjudicated:

| Site | Parameter | Disposition |
|---|---|---|
| `scan/coarse.py:63` `min(n_coarse, n)` | `--n-coarse` | Forced by `replace=False`; requested value recorded in `CoarseToFineResult.params["n_coarse"]` (`:101`), actual coverage in `frac_centers_scanned` (`:99`) — accounted |
| `intake/prep.py:180` `min(subsample.n, before)` | plan `subsample.n` | Log line records requested n AND `before -> after` rows (`:187-190`) — accounted |
| `dee/vknn.py:116` `min(k_prime, n)` | `--k-prime` | Forced (≤ n points exist); effective value returned as `VKNNResult.k_prime` ("effective k′ (clamped to n)", `:58`, set `:166`) — accounted |
| `dee/vknn.py:69` `cands[: max(int(m_prime), 0)]` | `--m-prime` | `m_prime > n_candidates` takes all (visible: `len(accepted)+len(rejected)` = candidates offered); **negative `m_prime` silently coerced to 0** → zero candidates, zero experiments, no error — F-C4 (noted) |
| `dee/observational.py:130` `min(n_folds, u)` | `n_folds` (not CLI-exposed) | Documented in the docstring (`:127`, `:145-146`) — accounted |
| `dee/bma.py:203` `n_folds = max(2, u)` | `n_folds` | Reduction recorded in the returned detail dict (`n_folds_requested`/`n_folds`, `:201-204`) — accounted |
| `iv/donors.py:201` `min(n_donors, candidates)` | `--n-donors` | Forced; help documents "default: all complete candidates"; ALL candidates stay ranked in `scores` — visible |
| `did/mdss.py:287-293` `exhaustive_max_values` (12) / `_MAX_EXACT_VALUES` (20) | scan cardinality | Loud `ValueError` refusal above 20, never silent (§A item 16) — positive example |
| `discover.py` `max_configs` budget | `--max-configs` | Explicit `skipped_budget` status per config (`:125`, `searched` coverage dict `:152`) — positive example |
| `validate/panel.py:116` `phi` clip to `[0, _PHI_MAX]` | internal AR(1) fit | Not a user parameter; model-fit stabilization |
| `q`, `windows`, `restarts`, `grid`, `t_side` | CLI | No clipping found anywhere; `suddds_scan` validates `restarts >= 1` (`:308-309`) and nonempty `windows` (`:335-336`); `knn_indices` raises on `k < 2` (`scan/neighborhoods.py:14-15`) |

## C. Docs accuracy execution log

Run 2026-07-12 (task 5). All runs in the session scratch dir
`/private/tmp/claude-501/…/ab012ec7-…/scratchpad/task5` (`$SCRATCH` below) — nothing written
under the repo tree (`git status` clean before this doc edit). Environment: repo venv
(Python 3.13, `dev`+`plot`+`report` extras installed), tectonic 0.x on PATH,
`NATEX_DATA` set to the local benchmark root for the `datasets` row. Inputs: `synth.csv`
generated exactly per the README heredoc (`make_synthetic(n=500, zeta=6.0, kind="binary",
rng=default_rng(0))`); IV panel `make_iv_synthetic(n=400, p=5, s=2, rng=default_rng(3))`;
SC panel `make_sc_synthetic(rng=default_rng(4))`.

| # | Doc | Command executed | Expected (as documented) | Observed | Verdict |
|---|---|---|---|---|---|
| 1 | README.md:124-135 | heredoc: `make_synthetic(n=500, zeta=6.0, kind="binary", rng=default_rng(0))` → CSV | demo CSV written | `$SCRATCH/synth.csv` written, columns `x0,x1,T,y` | PASS |
| 2 | README.md:55-60; skills/discover…/SKILL.md:39-43 | `uv run natex --help` | command table for all 9 commands | 9 commands listed with descriptions — EXCEPT `discover`, whose description is empty | PASS (nit = F-C1, fix scheduled task 7) |
| 3 | README.md:133-145 | `natex discover $SCRATCH/synth.csv --treatment T --outcome y --k 40 --q 49 --seed 0 --out …` | pasted block: `model=bernoulli max LLR=21.16 scan p=0.020`, top center `[0.757951 0.51275872]`, `placebo passed: True density p: 0.559`, `2SLS tau=2.047 CI=(1.024,3.070) weak_iv=False`, `results: …/out/results.json` | stdout matches the pasted block character-for-character; stderr additionally carries ~50 sklearn `OptimizeWarning: Unknown solver options: iprint` lines | PASS (noise = F-D5) |
| 4 | README.md:73-76 | inspect `results.json` | top-20 discoveries (center values, LLR, hyperplane normal, per-variable forcing influence), scan p, placebo/density, 2SLS+Wald effects, params/seed | 20 discoveries each `{center_z, llr, normal, forcing_influence}`; top-level `{discoveries, effects, params, scan, validation}`; `params.seed=0` | PASS |
| 5 | README.md:81-85 | `natex study … --backend null --seed 0 --out …` | writes `out/intake_report.json`, `out/prep_plan.json`, ranks candidate designs | all three files written (`intake_report.json`, `prep_plan.json`, `guidance_log.jsonl`); summary lines print candidates + paths | PASS |
| 6 | README.md:84,90; AGENTS.md:51 | `natex discover --plan …/intake_report.json --seed 0 --out …` | plan mode writes `out/discover_report.json`; coverage counts reported | `discover_report.json` written; stdout `scanned 1/1 configs (0 skipped by budget)`; report `searched` block has `n_scanned/n_skipped_budget/n_failed/n_invalid` | PASS |
| 7 | README.md:103-117 | `NATEX_DATA=… uv run natex datasets` | pasted 6-row block (names, found, rows, ok=True), exit 0 | all 6 rows identical to the pasted block (paths unabridged, as the doc's "truncated to …" note says); exit 0 | PASS |
| 8 | README.md:150-170 | Python API quickstart block (`Dataset.from_csv`, `lord3_scan`, `randomization_test`, `local_2sls`) | attributes `rep.p_value`, `est.tau/ci/first_stage_t/weak_instrument` | runs verbatim (CSV substituted); `p=0.01`, `tau=1.139`, ci tuple, `first_stage_t=8.83`, `weak_instrument=False` | PASS |
| 9 | README.md:192-194 | `natex debias $SCRATCH/synth.csv --treatment T --outcome y --m-prime 25 --out …` | writes `out/dee_result.json` (weights, effects table, grid predictions, diagnostics) | `dee_result.json` written; stdout reports experiments/w_debias/grid means | PASS |
| 10 | README.md:202-215 | `natex instruments $SCRATCH/iv.csv --treatment T --outcome y --pool z1,…,z5 --seed 0 --out …` | `out/instruments.json` with selection block (names, λ, loadings, first-stage F, weak flag), split sizes, estimation block | `instruments.json` keys `{caveat, estimate, params, selection, split}`; selection has `selected/lam/loadings/first_stage_F/weak`; honest split 200/200 printed | PASS |
| 11 | README.md:223-231 | `natex donors $SCRATCH/sc.csv --outcome y --unit unit --time time --treated-unit treated --t0 15 --out …` | `out/donors.json` with ranked scores, weights, gap by time, pre/post RMSPE, `att_post`, placebo block | `donors.json` keys `{att_post, diagnostics, donors, effect_by_time, params, placebo, post_rmspe, pre_rmspe, scores, t0, times, treated_unit, weights}` | PASS |
| 12 | README.md:238-243 | agent-flow block: `study` → `discover --plan` → `paper --bundle out --format md` → `brief --bundle out` | `paper/paper.md` and `research-brief.md` land in the bundle | `BUNDLE/paper/paper.md` and `BUNDLE/research-brief.md` written at the documented paths; banner + repeat-warning printed | PASS |
| 13 | README.md:245-250; skills/natex-write-paper/SKILL.md:10-13 | `natex paper --bundle $SCRATCH/demo-out --format md` (plain single-scan `results.json` bundle) | "accepts … a single-scan `results.json` from the plain `natex discover` path" | renders, but the paper is EMPTY of the run: no discovery rows, "No configuration was scanned, so no validation battery ran", no effects — while `results.json` holds 20 discoveries, scan p=0.020, validation, and 2SLS tau=2.047. `natex brief` on the same bundle likewise: "No configuration was scanned in this bundle" | FAIL → F-D1 |
| 14 | README.md:248-250; skills/natex-write-paper/SKILL.md:34-45 | `natex paper --bundle …/plan-out --format latex` (tectonic on PATH) | writes `paper.tex`, compiles `paper.pdf` when tectonic is installed | `paper.tex` + `paper.pdf` both written; paths printed | PASS |
| 15 | README.md:389-392 | `uv run python benchmarks/run_nig_curve.py --help` | script exists; `--kind real\|binary\|both`, `--label-noise` | help lists `--kind {real,binary,both}`, `--label-noise`, `--rhos`, `--out` etc. | PASS |
| 16 | AGENTS.md:46-56 | CLI-surface table, all 9 rows | key inputs + output filenames truthful | verified per rows 5-14 plus: `datasets` stdout-only/exit 0 (row 7); `fetch-data academic_probation --root …` → prints openICPSR instructions, exit 1 (login-gated, as documented); `discover` plain → `results.json`, plan → `discover_report.json` | PASS |
| 17 | AGENTS.md:63-71; SKILL.md:65-72 | launch `natex study $SCRATCH/synth.csv --backend agent --seed 0 --out $SCRATCH/agent-out` in background; poll `agent-out/guidance/requests/` | `0000_understand.json` appears under `WORKDIR=OUT/guidance`; EXACTLY five keys `task/payload/schema_hint/instructions/respond_to`; `task` in the documented six-value enum | file appeared; `sorted(keys) == [instructions, payload, respond_to, schema_hint, task]`; `task="understand"`; schema_hint matches the worked example (`required: ["shape"]`, same 8 properties); `--context` string forwarded verbatim in `payload.context` | PASS |
| 18 | AGENTS.md:72-76; SKILL.md:80-83 | hand-write answers: bare object for `understand` (one write), `{"content": {...}}` envelope for `prepare` | `respond_to` == `WORKDIR/responses/<same filename>`; both forms accepted | `respond_to` equality asserted True; bare form unblocked the run, envelope form unblocked the run | PASS |
| 19 | AGENTS.md:77-79 | measure unblock latency after each response write | polls every 0.5 s | next request appeared 0.31 s / 0.21 s after the response write — within one poll interval | PASS |
| 20 | AGENTS.md:80-82 | re-run the same `study --backend agent --out agent-out` | sequence continues after request files on disk; earlier questions never overwritten | first request of the re-run is `0003_understand.json`; `0000-0002` untouched | PASS |
| 21 | AGENTS.md:82-83; SKILL.md:152-154 | inspect `agent-out/guidance_log.jsonl` after both sessions | every request and response appended | 7 lines, each `{seq, task, backend, request, response, ts}` with full request payload/schema and full response; includes the one null-fallback entry (see note below) | PASS (note = F-D6) |
| 22 | AGENTS.md:151-157 | typical guided session: `study --backend agent` (answer while it blocks) → `discover --plan out/intake_report.json --seed 0 --out out/` | session completes; `intake_report.json` consumed | study finished < 1 s after the last answer; `intake_report.json`, `prep_plan.json`, `guidance_log.jsonl` all present; discover --plan ran to `discover_report.json` | PASS |
| 23 | skills/discover…/SKILL.md:156-166 | `natex discover --plan … --backend agent --seed 0 --out agent-out` (same serving protocol at discovery time) | discovery-time guidance tasks (`interpret_discovery`, `audit_assumptions`, `review_control_group`) served by the same file protocol | `0006_interpret_discovery.json` + `0007_audit_assumptions.json` written and served (both exactly five keys); `review_control_group` not triggered for an RDD-only plan (doc lists the possible tasks, fine); report carries `advisory` blocks; CLI prints each pending respond_to path | PASS |
| 24 | skills/discover…/SKILL.md:168-179 | inspect outputs per §6 | plan mode → `discover_report.json`; plain → `results.json`; effects report `tau/se/ci/first_stage_t/weak_instrument` | both filenames as documented (rows 3, 6); effects blocks carry exactly those fields (row 25 spot-check) | PASS |
| 25 | skills/natex-write-paper/SKILL.md:47-68 | banner + verify-numbers walk on the plan bundle's `paper.md` | banner verbatim; every rendered number matches the bundle JSON | banner "AI-generated draft — verify all claims before circulation" is line 3 of both papers; 10 spot-checked numbers all match `discover_report.json`: LLR 10.7↔10.690, MC p 0.02, 229 discoveries, 2sls tau −0.212↔−0.21200, SE 0.19↔0.18989, CI [−0.584, 0.16], first-stage t 2.21↔2.2145 + weak-IV flag, wald tau −0.138, density p 0.00454↔0.0045355, center (0.467, 1.73)↔(0.46667, 1.73180). BUT the doc says to check against `OUT/results.json`, which does not exist in a plan bundle (F-D4); and the Robustness section renders "Holm-corrected p = {}" (F-D3) | PASS with findings F-D3, F-D4 |
| 26 | skills/natex-lit-review/SKILL.md:13-30 | `natex brief --bundle plan-out`; rerun; `--out $SCRATCH/named-brief.md`; Python `research_brief(ResultsBundle.load(...), ...)` | writes `OUT/research-brief.md`, prints path; reruns byte-identical; `--out path.md` names the file exactly; Python API returns the path | all four observed: path printed, `cmp` of two runs identical, `named-brief.md` created, API returned `plan-out/research-brief.md` | PASS |
| 27 | skills/natex-lit-review/SKILL.md:32-47 | check brief sections | Discovery context, Discovered designs, Effect estimates, Validation status, numbered literature questions | headers present in order: `## Discovery context`, `## Discovered designs`, `## Effect estimates`, `## Validation status`, `## Literature questions for deep research` (numbered 1-N), plus `## Open questions` and `## How to use` | PASS |

Not executed (out of scope for a scratch run): `uv add git+https://…` (network install), the
`ln -s … ~/.claude/skills/` install line (writes to the user's home), the 600 s agent
timeout (only its documentation was checked), and full benchmark runs (help-only smoke per
the task protocol). The AgentBackend robustness path was exercised by accident and behaved
well: one deliberate-invalid answer (`budget` as int where `schema_hint` says object) was
rejected with the pydantic error printed and a fall-back to NullBackend heuristics, logged
as its own `guidance_log.jsonl` entry — fail-visible, statistics never fabricated.

## D. Spec completeness matrix

Walked 2026-07-12 (task 6) against the design spec
(`docs/superpowers/specs/2026-07-10-natex-design.md` in the RDD workspace). Dispositions:
**Implemented** (path:line / test) · **Deviation-by-design** (no change; real surface
documented in shipped docs) · **Future work** (row in `docs/status/future_work.md`) ·
**Deviation (finding)** (register row). The §4 API contract was executed live in a scratch
Python session (see the F-E1 row for the transcript facts).

| Spec section | Promise | Disposition | Evidence / future_work entry |
|---|---|---|---|
| §1.1 | RDD discovery (LoRD3) | Implemented | `src/natex/rdd/lord3.py:64` `lord3_scan`; method card `docs/method_cards/lord3.md`; backtests `tests/backtests/test_test_score.py`, `test_academic_probation.py`, `test_ed_inpatient.py` |
| §1.1 | DiD discovery (SuDDDS, first public implementation) | Implemented | `src/natex/did/suddds.py:265` `suddds_scan`; `docs/method_cards/suddds.md`; Prop 99 backtest `tests/backtests/test_prop99.py` |
| §1.1 | DEE discontinuity debiasing | Implemented | `src/natex/dee/debias.py:122` `dee_debias` (VKNN repair, causal-forest adapter, two-stage GP, BMA); `docs/method_cards/dee.md`; `tests/test_dee_benchmarks_small.py` |
| §1.1 | Instrument/donor-pool search (Chen et al. roadmap) | Implemented | `src/natex/iv/search.py:104` `select_instruments` (Lasso first stage + strength/exclusion diagnostics), `src/natex/iv/donors.py:94` `select_donors` (pre-trend scoring); `docs/method_cards/iv_sc.md` |
| §1.2 | Full validation battery: randomization (multiplicity via max-LLR null), density, placebo, balance | Implemented | `src/natex/validate/randomization.py:35` (max-LLR fitted-null MC = family-wise multiplicity control), `density.py:25`, `placebo.py:42` (covariate balance as intercept-continuity placebos, Holm — audit item 3 supersedes the spec's Bonferroni wording), `panel.py` for DiD; wired in `src/natex/discover.py:266-297` |
| §1.3 | Multiple estimators + honest splits | Implemented | `src/natex/estimate/local2sls.py:116` (frozen-side 2SLS), `:159` `wald_estimate`, `iv2sls.py` (2SLS/J/AR), `did/effects.py:176` `did_effect`; `validate/honest.py:9` `honest_split` (wired in `iv/pipeline.py:100`; discover-pipeline wiring = F-A2 → future_work row "honest-split wiring") |
| §1.4 | LLM analyst (Stage 0 + hooks), agent and API modes, fully functional with no LLM | Implemented | `src/natex/intake/analyst.py` `study`; backends `src/natex/llm/backends.py` (`NullBackend` default), `agent.py` (`AgentBackend`), `api.py` (Anthropic/Gemini); no-LLM path verified live in §C rows 5-6 (`--backend null`) |
| §1.5 | Paper pipeline: results → figures → LaTeX (tectonic) or Google Docs; paperbanana; deep-research review | Implemented (Google Docs manual, paperbanana adapter-only) | `src/natex/report/bundle.py`, `figures.py`, `paper.py` (tectonic compile verified live §C row 14), `paperbanana.py`, `research_brief.py`; Google Docs → future_work row "Google Docs API export"; `--diagrams` flag → future_work row "--diagrams CLI flag" |
| §1.6 | Backtest everything (known truths + papers' synthetic benchmarks) | Implemented | `tests/backtests/` (6 datasets, 31 passed + 1 xfail); `benchmarks/run_nig_curve.py`, `run_did_curves.py`, `run_dee_sim.py`, `run_iv_selection.py`; §8 rows below |
| §1 non-goals | No streaming, no non-tabular, no R interop, staggered DiD only as pluggable backend | Implemented (respected) | No streaming/text/image/R code exists (pure-Python core deps only, `pyproject.toml:9-16`); staggered backend boundary exactly as promised: `did/effects.py:316-319` protocol + `mean_gap` only (F-B3 → future_work row) |
| §2 | Repo/package/CLI named `natex`; verify PyPI availability | Deviation-by-design + Future work | Repo `github.com/HaukeHillebrandt/natex`, import `natex`, CLI `natex` ✓; PyPI dist name `natex-discovery` (plain `natex` unavailable), publish pending → future_work row "PyPI publish"; README:38-40 and `docs/release_notes/v0.1.0.md:97-99` document the real names |
| §3 tree | `data/` `intake/` `scan/` `rdd/` `did/` `dee/` `iv/` `validate/` `estimate/` `llm/` `report/` `cli.py` | Implemented | All present under `src/natex/` (only drift: `mdss.py` lives in `did/` not `scan/` — it is SuDDDS-specific; `scan/` holds the shared neighborhoods/statistics/geometry/coarse core as promised) |
| §3 tree | Core deps incl. statsmodels + matplotlib | Deviation-by-design | House rules win (phase-1 global constraints): core = numpy/scipy/pandas/scikit-learn/typer/pydantic (`pyproject.toml:9-16`); matplotlib is the `plot` extra (`:19`); statsmodels never needed (own HC1/2SLS in `estimate/`); README:38-46 documents the real dep story |
| §3 tree | CLI `discover\|validate\|estimate\|report\|paper\|fetch-data` | Deviation-by-design | Real surface is 9 commands (`src/natex/cli.py`): `datasets fetch-data study discover debias instruments donors paper brief`; validate+estimate run inside `discover` (`discover.py:266-297`), `report` folded into bundle outputs (`docs/status/phase-report-paper.md:129`). Grep confirms NO shipped doc promises `natex validate`/`natex estimate`/`natex report` (only prose "natex reports p-values"). → future_work row "validate/estimate/report subcommands" |
| §3 tree | `skills/` three skills + AGENTS.md | Implemented | `skills/discover-natural-experiments/`, `natex-write-paper/`, `natex-lit-review/`; AGENTS.md at repo ROOT (not under `skills/`) — where non-Claude agents actually look; §C rows 16-23 executed it |
| §3 tree | `tests/`: unit + property (permutation/LLR≥0/invariance) + legacy-bug regressions + CLI smoke | Implemented | LLR≥0 + permutation invariance `tests/test_did_statistics.py:331`, `test_statistics_bernoulli.py:32`, scale invariance `test_statistics_normal.py:28`; legacy-bug regressions per class (§A items 20-24 rows); CLI smoke `tests/test_cli*.py` |
| §3 tree | `benchmarks/` reproducing paper curves + backtest harness | Implemented | `benchmarks/` (4 runner scripts + README; `out/` gitignored); marker `backtest` (`pyproject.toml:51`), harness `tests/backtests/conftest.py` |
| §3 tree | `legacy/` annotated Herlands copy | Deviation-by-design | Intentionally dropped: never commit third-party code (global constraint). The bug cross-reference lives in `docs/method_cards/lord3.md:57-63` (+ per-card deviation sections) and `docs/math_audit_final.md`, each legacy bug class pinned by a regression test (§A items 20-24). → future_work row "legacy/ directory" |
| §3 tree | `docs/`: method cards, paper-extraction notes, user guide | Implemented (notes stay external) | 5 method cards in `docs/method_cards/`; governing math copied in (`docs/math_audit_final.md`); extraction notes remain in the private RDD workspace (papers are copyrighted; cards are the distillation); README is the user guide (copy-paste quickstart verified §C) |
| §3 tree | CI: ruff + mypy (loose) + pytest 3.11-3.14 | Future work (mypy) | `.github/workflows/ci.yml` runs ruff + pytest on 3.11/3.12/3.13/3.14 ✓; NO mypy step exists and no shipped doc claims typechecking → future_work row "mypy in CI" |
| §4 | `natex.study(csv, context=, guidance=)` → intake.report / .prep_plan / .prepare() | Implemented (drift: return IS the report; explicit `rng` required) | Live run: `natex.study(...)` ✓ top-level, `context=`/`guidance=`/`out=` ✓; the spec's bare call raises `ValueError` ("pass an explicit numpy Generator") — `rng=` is mandatory, documented `llm_analyst.md:19,174`; returns `IntakeReport` itself (no `.report` attribute), `.prep_plan` ✓, `.prepare()` → `Dataset` ✓ (F-E1) |
| §4 | `natex.DatasetSpec.from_csv(...)` | Deviation-by-design | Live run: `DatasetSpec.from_csv` does not exist; the constructor is `natex.Dataset.from_csv(path, treatment=, outcome=, forcing=, covariates="auto")` (same kwargs; `DatasetSpec` is the pydantic column-mapping carried by `Dataset`). README:159 and all docs teach the real form (F-E1) |
| §4 | `natex.discover(data, design="auto", guidance=, seed=0)` | Deviation-by-design | Live run: `seed=0` raises TypeError; real signature takes `rng: np.random.Generator` (house rule: one Generator everywhere; CLI converts `--seed` once at `cli.py:351`). `design="auto"` ✓, `guidance=` ✓. Documented: `docs/method_cards/llm_analyst.md:26` (F-E1) |
| §4 | `result.discoveries` / `[0].effect` / `result.to_report("out/")` | Deviation-by-design | Live run: `DiscoverReport` has `configs`/`searched`/`best_index`/`best()`; effects live in `best().summary["effects"]` (`2sls`/`wald` with tau/se/ci); reports are written by `discover(..., out=)` → `discover_report.json`, papers via `ResultsBundle` + `natex paper`. Ranked per-scan `discoveries[0]` DOES exist on `LoRD3Result`/`SuDDDSResult` (README:150-170 teaches exactly that route). No shipped doc echoes the spec forms (F-E1) |
| §4 rules | One Generator per stochastic step; identical seed ⇒ identical output | Implemented | §B1 sweep (48 `rng` params, no global seeding); bitwise replica parity `tests/test_geometry.py:112` |
| §4 rules | Discovery never touches y; optional honest split | Implemented (honest wiring partial) | y-blindness: `lord3_scan` reads only (X, T, Z); pinned by `tests/test_lord3.py:33` `test_outcome_never_read` (NaN-poisoned outcome; permuted-outcome analog cited `dee.md:162`); `honest_split` optional ✓ but auto-wired only in IV (F-A2 → future_work row) |
| §4 rules | Plain dataclasses, JSON-serializable, no pickle | Implemented | §B3 table; `grep pickle src/natex` empty; `jsonutil.jsonable` |
| §4 rules | Extras: `[gp]` `[ml]` `[report]` (jinja2, paperbanana) `[llm]` | Implemented (drift: paperbanana its own extra, + `plot`) | `pyproject.toml:18-33`: `gp`/`ml`/`llm` as promised; `report` = jinja2 only; `paperbanana` split out (needs its own provider key); `plot` added for matplotlib. README:40-46 documents the real extras |
| §5.1-5.8 | Corrections implemented with regression tests + method-card notes | Implemented | One-to-one with the audit rows: §5.1→§A 2.1/2.2, §5.2→2.3, §5.3→2.4/§3-adopted-10, §5.4→2.11-2.16/2.18, §5.5→2.8/2.9/2.7/§3-adopted-7/8 + train_data/projection (§3-rejected-3), §5.6→2.21/2.22/2.23/2.24/2.6, §5.7→2.20, §5.8→§B2 sweep (NaN-never-0.0) + §A 2.16 (loud refusal, no silent all-points variant). Each §A row cites code + test + card |
| §6a | Stage 0: profile → understand → prepare (declarative PrepPlan) → search plan | Implemented (escape hatch out of scope) | `src/natex/intake/profiler.py` (deterministic), `analyst.py` (understand/classify via backend, Null heuristics without LLM), `prep.py` (declarative executor, versioned, library-executed), `plans.py` (`SearchPlan` ranked candidates + budget); serialized+re-runnable verified §C rows 5-6. Sandboxed pandas escape hatch documented OUT of scope (`llm_analyst.md:200`) → future_work row |
| §6b | Targeted first, exhaustive still; bundle reports what was/wasn't searched | Implemented | `src/natex/discover.py:125,152` (`skipped_budget` status + `searched` coverage dict `n_scanned/n_skipped_budget/n_failed/n_invalid`), verified live §C row 6 ("scanned 1/1 configs (0 skipped by budget)") |
| §6c | Five in-scan hooks incl. `propose_forcing_variables`/`propose_candidate_events` | Deviation-by-design (documented) | Implemented tasks: `understand/prepare/search_plan/interpret_discovery/audit_assumptions/review_control_group` (`llm/backends.py:17-24`); the two mid-run proposal hooks are folded into Stage-0 `search_plan` — deviation documented at `docs/method_cards/llm_analyst.md:43,202` and `docs/status/phase-llm-analyst.md:118` → future_work row "mid-run proposal hooks". All guidance logged (`guidance_log.jsonl`, §C row 21); propose-and-veto only (advisory blocks, §C row 23) |
| §6d | Scale: KD-tree/ANN, vectorized LLR, dedup, coarse-to-fine, early pruning, parallelism | Implemented except ANN (deliberate) and parallelism | Exact cKDTree `scan/neighborhoods.py:16`; vectorized LLR `scan/statistics.py:132-222`; partition-equivalence dedup `neighborhoods.py:41-49`; coarse-to-fine `scan/coarse.py`; homogeneous-treatment pruning `rdd/lord3.py:98-105`; replica geometry reuse `scan/geometry.py`. ANN: opt-in only per audit §3 rejected-4 downgrade, not shipped (§3-decisions row) → future_work row. Cross-neighborhood parallelism absent (serial, bitwise-deterministic) → F-E2 + future_work row |
| §7.1 | `natex report` → bundle with 5 standard figures | Implemented (folded; LLR-ranking figure absent) | Bundle+figures via `ResultsBundle.save` + `report/figures.py`: discovery map (`discovery_scatter`, LLR-colored), McCrary (`density_hist`), pre-trend (`pretrend_plot`), forest (`effect_forest`); LLR ranking delivered as the scatter's color map + ranked top-20 tables in `results.json`/paper, no dedicated bar figure (F-E3). CLI fold-in: `docs/status/phase-report-paper.md:129` |
| §7.2 | `natex paper --format latex\|gdoc`, tectonic compile; Google Docs export | Implemented (md/latex; gdoc manual) | `cli.py:889-898` `--format md\|latex`, tectonic compile verified live (§C row 14); Google Docs deliberately manual (README:290-294, write-paper SKILL §4) → future_work row |
| §7.3 | `--diagrams paperbanana` | Deviation-by-design | Diagrams ship as the Python adapter `report/paperbanana.py` + `paperbanana` extra (README:252-258); no CLI flag exists and no doc promises one → future_work row |
| §7.4 | Deep-research lit review via the user's skill | Implemented | `natex brief` / `research_brief` (`report/research_brief.py`) produce the handoff; `skills/natex-lit-review/SKILL.md` hands to the USER'S deep-research tooling — the deep-research path makes no API call (boundary respected: `report/research_brief.py` imports only stdlib + natex; reruns byte-identical, §C row 26) |
| §7.5 | Human-in-the-loop: draft is a draft | Implemented | AI-draft banner line 3 of every paper (verified §C row 25); `skills/natex-write-paper/SKILL.md:47-68` verify-numbers walk; `cli.py:935` "verify everything it returns" |
| §8 | test_score_2012: cutoff ±2, τ in CI, density passes | Implemented | `tests/backtests/test_test_score.py` (passes in the 31-green run of record) |
| §8 | AcademicProbation: cutoff; forcing var ranked #1 of 4 | Implemented | `tests/backtests/test_academic_probation.py` |
| §8 | ED visits ages 19 & 23; Inpatient age 23 (small n) | Implemented | `tests/backtests/test_ed_inpatient.py` |
| §8 | Egger–Köthenbürger ≥2 statutory thresholds (stretch) | Implemented (stretch failure documented per §10) | Core: `tests/backtests/test_egger_koethenbuerger.py:16` passes "with zero tuning — no xfail needed"; the strict decoy-free-support variant is the documented failure: `tests/backtests/test_prop99_donors.py:199-204` pinned `xfail` (the run of record's 1 xfailed) — exactly the spec-§10 "failure is a documented finding" |
| §8 | Prop 99: (CA, 1989); τ̂ vs Table 6.1 | Implemented | `tests/backtests/test_prop99.py` (magnitude caveat documented `docs/status/phase-3.md:76`) |
| §8 synthetic | NIG-vs-ζ, P/R/F curves, DEE sim-1, legacy parity where correct; CI runs small suites | Implemented | `benchmarks/run_nig_curve.py`, `run_did_curves.py`, `run_dee_sim.py`; small seeded versions in CI (`tests/test_benchmarks_small.py`, `test_did_benchmarks_small.py`, `test_dee_benchmarks_small.py`); parity only against natex-internal references/paper truths (audit item 20, §A row) |
| §9 | Phases 1-8 each gated + status doc | Implemented | README Project status table rows 1-8 all Done (pinned by `tests/test_readme_release.py:60`); `docs/status/phase-*.md` one per phase; PyPI (phase-8 option) pending → future_work row |
| §10 | SuDDDS hyperparameters exposed + calibrated | Implemented | `suddds_scan(windows=, restarts=, min_side=, exhaustive_max_values=, method=, model=...)` all parameters (`did/suddds.py:265-309`); defaults calibrated on synthetic curves (`benchmarks/run_did_curves.py`); deviations documented `suddds.md` |
| §10 | DEE heavy GP → optional extra, small-N defaults, CPU-only | Implemented | `gp` extra (`pyproject.toml:28`); tests skip gracefully without it (`tests/test_dee_optional.py`); CPU torch works (CI never installs gp) |
| §10 | rdrobust/rddensity lag → own fallbacks behind interfaces | Implemented (own-only) | Own implementations ARE the implementation (`estimate/local2sls.py`, `validate/density.py`); bridges never wired → future_work rows (rdrobust bridge, rddensity bridge; F-A2) |
| §10 | LLM-bias mitigation: logged, optional, identical validation; guided-vs-unguided benchmark | Implemented | Logging §C row 21; identical battery for guided runs (`discover.py` same path); phase-6 eval `src/natex/guidance_eval.py` + `benchmarks/guidance_eval.py` (blinded arms, §B1 row) |
| §10 | Egger stretch: failure = documented finding, not blocker | Implemented | See §8 Egger row (pinned xfail is the documentation) |
| §10 | Non-engineer README: copy-paste-runnable; skills explain plainly | Implemented | Every README/skill command executed verbatim in §C rows 1-27 (2 findings F-D1/F-D3 already registered); skills' plain-language framing verified §C rows 25-27 |

Every spec section (§1-§10) appears above; no promise left unclassified. New findings from
this walk: F-E1 (API-contract drift, documented — no change), F-E2 (no cross-neighborhood
parallelism), F-E3 (no dedicated LLR-ranking figure) — all noted, none doc-wrong, since every
shipped doc teaches the real surface.

## Findings register

| ID | Source | Severity | Where | Defect | Resolution |
|---|---|---|---|---|---|
| F-A1 | task 2, audit §2 item 3 | must-fix | `src/natex/validate/placebo.py:59-66` | The inline Holm step-down is not NaN-aware: a NaN placebo p (reachable — a dummy exactly collinear with the side indicator gives se=0 → t=NaN → p=NaN at `:56-58`) inherits the running max as a fabricated finite adjusted p (silently counted by `passed`), or yields a literal 0.0 when the NaN sorts first, and inflates the Holm multiplier m for real p-values. Verified by direct execution of the `:59-66` logic with `{a: .02, b: NaN, c: .5}` → `p_holm[b] = 0.06`, and `{b: NaN, a: .02}` → `p_holm[b] = 0.0`. Violates NaN-never-0.0 and item-3 multiplicity correctness. NaN-aware implementations already exist at `src/natex/did/effects.py:559-568` and `src/natex/validate/panel.py:311-325`; repair = reuse one + pinning test (also covers the untested categorical/Holm path). | |
| F-A2 | task 2, audit §2 item 6 | noted | `src/natex/validate/density.py:25`; `src/natex/cli.py:364`; `src/natex/discover.py:269` | Audit prescribes "rddensity on signed distance along the frozen normal from an honest split": natex implements a binned-Poisson McCrary-style analogue (not rddensity's local-polynomial estimator — core-deps constraint, deviation documented in `density.py:1-5` and `lord3.md:54-56`), and the `study`/`discover` pipelines run it on the full discovery sample; `honest_split` exists (`validate/honest.py`) but is wired only into `iv/pipeline.py:100`. Mitigation: documented as a falsification test only, never confirmatory. | |
| F-A3 | task 2, audit §2 item 9 | noted | `src/natex/dee/debias.py:148-150,208-209` | The `smooth_noise_stage1=False` opt-out feeds raw HC1 SE² directly to the GPs as known per-point noise — the pattern audit item 9 forbids. It is a documented, non-default comparison mode; the default (`True`) uses the adopted chi-square measurement model (`dee/noise.py`). | |
| F-B1 | task 3, audit §2 typos block | must-fix (doc) | `docs/method_cards/lord3.md`, `docs/method_cards/suddds.md`, `docs/method_cards/dee.md` | Six audit-listed pure typos have no method-card mention (plan task-3 protocol: every listed typo must appear in the relevant card): (1) Eq 5.21 inverse-logit + log μᵢ — documented only in the module docstring `src/natex/data/synthetic.py:1-2`, absent from `lord3.md`; (2) thesis xref 6.26→5.22 — absent from `suddds.md`; (3) DEE Codex #31 σᵢ SD-vs-variance in the LOOCV prose, (4) #34 Algorithm 2 returns wrong tuple type, (5) #35 Assumption 4 ill-typed independence → conditional mean independence, (6) #36 Appendix F.2 α_Y→α_B — all absent from `dee.md`. Repair = add the missing lines to each card's typo section (doc-only; no code impact). | |
| F-B2 | task 3, audit §3 adopted 1 | noted | `src/natex/scan/statistics.py:39-60`; `src/natex/rdd/lord3.py:113` | The adopted unknown-variance GLR `k/2·log(RSS0/RSS1)` is implemented and tested (`tests/test_statistics_normal.py:40`) but not selectable from `lord3_scan` or the CLI; the only wired Normal scan kernel is the heteroskedastic plug-in `normal_llr_all_splits` — the audit's adopted default-vs-option polarity is inverted. The plug-in path is the thesis/KDD-parity choice and its degeneracy guards are documented; wiring the GLR as a `statistic=` option is future work. | |
| F-B3 | task 3, audit §3 adopted 9 | noted | `src/natex/did/effects.py:316-319,345` | Staggered-adoption group-time ATT (Callaway–Sant'Anna) backend is not shipped: the `DiDEstimatorBackend` protocol hook exists with only `mean_gap` registered; documented as a future backend (`docs/method_cards/suddds.md:185-186`, deviation 7). Future-work candidate. | |
| F-B4 | task 3, audit §2 item 19 | noted | `src/natex/did/suddds.py:313-316`; `src/natex/did/background.py:134-139` | No Poisson variant for count treatments: `model="auto"` maps binary theta → Bernoulli and everything else → normal, so a count treatment gets the Normal-residual model. Documented future work (`docs/method_cards/suddds.md:74-75,179-180`); the continuous-treatment estimand itself is covered by dose normalization (`did/effects.py:180-243`). Future-work candidate. | |
| F-B5 | task 3, audit §3 adopted 5 | noted | `src/natex/validate/` | The decision's "step-down inference offered but labeled approximate" clause is not implemented at the discovery level: no per-discovery step-down selective-inference procedure exists. Holm step-down is offered only inside the validation batteries (`validate/panel.py:311-325`, `did/effects.py:559-568`, `validate/placebo.py:59-66`); the discovery-level guarantees are the max-LLR fitted-null calibration and the honest split (the decision's PRIMARY guarantee, implemented). No doc claims a step-down guarantee, so nothing is doc-wrong. Future-work candidate. | |
| F-B6 | task 3, audit §3 adopted 6 | noted | `docs/method_cards/dee.md:159-161` | Overlap-aware influence-function/bootstrap-covariance aggregation is deferred (documented: "considered and deferred"); the implemented behavior is the decision's own simple default — disjointness by construction via VKNN (`src/natex/dee/vknn.py:75-141`), and no overlap-fraction weighting exists anywhere. Future-work candidate. | |
| F-C1 | task 4, check B4 | must-fix | `src/natex/cli.py:281` | `def discover(...)` has no docstring, so `natex --help` shows an empty description for `discover` — every other command has one. (= plan's pre-confirmed nit 2; fix scheduled in task 7.) | |
| F-C2 | task 4, check B4 | must-fix (doc) | `src/natex/cli.py` (`study`, `discover`, `debias`, `instruments`, `donors`) | 17 options lack `help=` text (plan check: "every option has `help=`"): study `seed`,`out`; discover `outcome`,`k`,`seed`,`out`; debias `treatment`,`seed`,`out`; instruments `treatment`,`seed`,`out`; donors `outcome`,`unit`,`time`,`treated_unit`,`out`. `--seed`/`--out` are uniformly undocumented on the data commands; required options like debias/instruments `--treatment` and the donors panel columns show bare `[required]` with no description. Repair = one-line `help=` per option, shared spellings kept identical across commands. | |
| F-C3 | task 4, check B1 | noted | `src/natex/rdd/lord3.py:64-72` | `lord3_scan` accepts `rng: np.random.Generator \| None = None` but the body never consumes it — the scan is fully deterministic; callers (`discover.py:263`, `cli.py:360,450`, `validate/randomization.py:71`) pass `rng=rng` into a dead parameter, implying nonexistent stochasticity. No correctness impact (nothing is seeded or skipped). Repair candidates: drop the parameter, or document it as accepted-for-interface-uniformity in a docstring (`lord3_scan` currently has none). | |
| F-C4 | task 4, check B6 | noted | `src/natex/dee/vknn.py:69` | `_ordered_candidates` clamps a negative `m_prime` to 0 (`cands[: max(int(m_prime), 0)]`): a nonsensical `--m-prime -5` silently yields zero candidates and zero experiments instead of raising ValueError like the module's other guards. Fail-visible downstream (empty `experiments`, NaN CATE) but a silent coercion of a user budget. (`m_prime > n_candidates` taking all candidates is benign and visible via `len(accepted)+len(rejected)`.) | |
| F-C5 | task 4, check B3 | noted | `src/natex/iv/donors.py:57-80` | `DonorSelectionResult` names its effect `att_post`/`effect_by_time` rather than `tau`, and carries no `se`/`ci` (inference is the `sc_placebo_test` RMSPE-ratio p, per Abadie SC practice). Deliberate domain-standard naming, consistent across the SC vertical (CLI output, skills, method card); adjudicate against the tau/se/ci convention rather than rename mechanically. | |
| F-D1 | task 5, §C row 13 | must-fix | `src/natex/report/bundle.py:160-182`; `src/natex/report/paper.py:454`; `src/natex/report/research_brief.py:73-76` | `natex paper`/`natex brief` on a plain single-scan bundle silently render EMPTY documents: `ResultsBundle.from_scan_payload` stores the whole payload under `results["scan"]` with `configs=None`, but `_paper_context` and `_scanned_configs` read ONLY `results["configs"]` — so a bundle whose `results.json` holds 20 discoveries, scan p=0.020, a full validation block, and 2SLS tau=2.047 renders "No configuration was scanned, so no validation battery ran" / "No configuration was scanned in this bundle". README.md:245-247 explicitly promises this input form ("a single-scan `results.json` from the plain `natex discover` path"), and both skills point users at exactly such bundles. Repair = adapt the scan payload into a one-config view in `from_scan_payload` (test-first). | |
| F-D2 | task 5 (= plan known nit 1) | must-fix (doc) | `docs/method_cards/llm_analyst.md:106,128,132`; `docs/status/phase-llm-analyst.md:90` | Stale `out/agent/...` example paths: the agent-backend default workdir was renamed to `OUT/guidance` in phase skills-docs (verified live in §C rows 17-18: requests really land under `out/guidance/requests/`), but these four doc lines still say `out/agent/...`. Fix scheduled in task 7. | |
| F-D3 | task 5, §C row 25 | must-fix | `src/natex/report/paper.py:439-440`; rendered via `templates/paper.md.j2:127`, `templates/paper.tex.j2:161` | An empty `placebo_holm` dict renders as the literal Python repr "Holm-corrected p = {}" in the paper's Robustness prose (observed in a real render): `_validation_block` handles a NON-empty holm dict (`:431-438`) and falls through to `_fmt(holm)` for `{}`, which str()s the dict instead of emitting the "—" placeholder the docs promise for missing values (README.md:278-279 "Missing numbers render as '—', never `nan`"; write-paper SKILL.md:66-68). Also surfaces that a vacuous placebo battery (zero testable covariates) prints "passed" — worth a caveat word in the template. Repair = render `_EM` for an empty/None holm dict + pinning test. | |
| F-D4 | task 5, §C rows 25-26 | must-fix (doc) | `skills/natex-write-paper/SKILL.md:10-13,56-61`; `skills/natex-lit-review/SKILL.md:10-12,19-21` | Both skills tell the user the finished bundle "contains `results.json`" and to verify "against `OUT/results.json` — the single source every rendered number comes from" / "built from `OUT/results.json` alone" — but the bundle produced by the sibling skill's own flow (`natex discover --plan`, discover-natural-experiments SKILL.md §5) contains `discover_report.json` and never a `results.json` (verified: plan bundle after `paper`+`brief` holds no `results.json`). The verify-numbers instruction cannot be followed verbatim on plan-mode bundles. Repair = name both files in the two SKILL.md files (or have the bundle loader save a canonical `results.json`, which would also close F-D1). | |
| F-D5 | task 5, §C row 3 | noted | environment (sklearn 1.x + scipy ≥1.15), surfaces in every documented `discover`/`study` run | ~50 lines of `sklearn OptimizeWarning: Unknown solver options: iprint` flood stderr on every scan with current dep versions (sklearn's LogisticRegression passes `iprint` to a scipy L-BFGS that dropped it), while README's pasted console blocks (:109-117, :139-145) are clean. Not a natex defect and stdout matches the docs byte-for-byte; candidate cosmetic fix = a targeted `warnings.filterwarnings` around the sklearn fit or a dep floor note. | |
| F-D6 | task 5, §C row 21 | noted | `src/natex/llm/agent.py:46-51` (filename seq = files on disk) vs `src/natex/llm/log.py:30` (log seq = `n_entries`) | After an invalid agent response triggers the null-backend fallback, the `guidance_log.jsonl` `seq` counter and the request FILENAME counter diverge permanently (observed: fallback consumed log seq 3 with no request file, so the re-run's request `0003_understand.json` logs as seq 4, `0004_prepare` as seq 5, …). No documented claim is violated (AGENTS.md:80-82 ties sequencing to request files only, and the log stays complete), but the off-by-one makes request-file-to-log-entry reconciliation confusing in reproducibility audits. Candidate: log the request filename in each entry. | |
| F-E1 | task 6, spec §4 live run | noted | `src/natex/data/spec.py` (`Dataset.from_csv`); `src/natex/intake/analyst.py` (`study`); `src/natex/discover.py` (`discover`, `DiscoverReport`) | Spec §4 API-contract drift, verified by executing the contract in a scratch session: `natex.DatasetSpec.from_csv` does not exist (real constructor: `natex.Dataset.from_csv`, same kwargs; `DatasetSpec` is the pydantic column mapping carried by `Dataset`); `natex.discover(..., seed=0)` raises TypeError and `natex.study(...)` without `rng=` raises ValueError (real: both require an explicit `rng=np.random.Generator` per the one-Generator house rule; the CLI converts `--seed` once); `study` returns the `IntakeReport` itself — no `.report` attribute (`.prep_plan`/`.prepare()` work as promised); `DiscoverReport` carries `configs`/`best()` not `.discoveries`, effects live at `best().summary["effects"]`, and there is no `.to_report()` (reports via `discover(out=)` → `discover_report.json` + `ResultsBundle`/`natex paper`). Every shipped doc teaches the real surface (README:150-170, `docs/method_cards/llm_analyst.md:19,26,174`) and none echoes the spec forms — per the plan rule this is a no-change deviation-by-design, recorded for completeness. | |
| F-E2 | task 6, spec §6d | noted | `src/natex/rdd/lord3.py:93-96` (serial center loop); `src/natex/dee/forest.py:93` | Cross-neighborhood parallelism promised by spec §6d is not implemented: the scan iterates centers serially (bitwise determinism prioritized; the shipped §6d speedups are vectorized LLR, geometry caching across replicas, coarse-to-fine, homogeneous-treatment pruning, exact k-prefix reuse). Only econml's forest exposes `n_jobs` (serial by default for determinism). No shipped doc claims parallelism, so nothing is doc-wrong. Future-work candidate (row in future_work.md). | |
| F-E3 | task 6, spec §7.1 | noted | `src/natex/report/figures.py:331-411` | The spec's five standard figures ship as four: discovery map (`discovery_scatter`, LLR-colored), McCrary (`density_hist`), pre-trend (`pretrend_plot`), effect forest (`effect_forest`); a dedicated LLR-ranking figure does not exist — ranking is delivered by the scatter's LLR color map plus the ranked top-20 tables in `results.json`/the paper. No shipped doc promises such a figure. Future-work candidate (row in future_work.md). | |

## Fixes applied

## Run of record
